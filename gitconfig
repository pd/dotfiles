[user]
	name = Kyle Hargraves
	email = pd@krh.me
[color]
	ui = auto
[color "branch"]
	current = green
	local = yellow
	remote = yellow reverse
[color "diff"]
	meta = white bold
	frag = cyan bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[core]
	excludesfile = ~/.gitignore
[status]
	submodulesummary = true
	relativePaths = false
[github]
	user = pd
	token = 4b7c4bde82b4040e469cbd07a463c5bc
[alias]
	b   = branch
	br  = branch
	ba  = branch -a
	bav = branch -av
	ci  = commit
	co  = checkout
	cot = checkout -t
	cp  = cherry-pick
	d   = diff
	ds  = diff --cached
	dhd = diff HEAD
	f   = fetch
	m   = merge
	rb  = rebase
	re  = reset
	reh = reset --hard
	ru  = remote update
	rup = remote update --prune
	s   = status
	st  = status
	sh  = show
	sm  = submodule
	l   = log --date=short --pretty=format:'%C(bold magenta)%h %Creset%C(green)%cd%Creset %C(white)%aN%Creset %s %C(yellow)%d'
	lg  = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
	stash-unstaged = stash --keep-index
[push]
	default = upstream
