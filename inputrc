# tab completion:
#   don't show dotfiles
#   don't beep, just show possible options
#   use / and * to show file types
set match-hidden-files off
#set show-all-if-ambiguous on
set visible-stats on

set editing-mode vi
set keymap vi

